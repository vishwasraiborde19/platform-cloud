# add single key	"kubectl create configmap <configmapName>
   --from-literal=<key>=<value>"
# add multiple keys	"  kubectl create configmap <configmapName>
   --from-literal=<key>=<value>
   --from-literal=<key>=<value>"
  # add from file	"   kubectl create configmap <name>
   --from-file=<file-name>"
