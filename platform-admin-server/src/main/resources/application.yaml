server:
  port: 9000

spring:
  application:
    name: platform-admin-server


management:
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  client:
    enabled: false
    service-url:
      defaultZone: http://192.168.0.10:8761/eureka
    register-with-eureka: false